# SOLID Principles

## 🟦 S --- Single Responsibility Principle (SRP)

Един клас = една единствена отговорност.\
Класът трябва да прави едно нещо и да го прави добре.

### ❌ Лош пример:

``` ts
class UserService {
  createUser() {}
  sendEmail() {} // не е работа на UserService
}
```

### ✅ Добър пример:

``` ts
class UserService {
  createUser() {}
}

class EmailService {
  sendEmail() {}
}
```

------------------------------------------------------------------------

## 🟩 O --- Open/Closed Principle (OCP)

Класовете трябва да са отворени за разширяване, но затворени за
промяна.\
Добавяш ново поведение без да променяш стария код.

### Пример:

``` ts
abstract class Shape {
  abstract area(): number;
}

class Circle extends Shape {
  area() { return 3.14 * 2 * 2; }
}

class Square extends Shape {
  area() { return 4 * 4; }
}
```

👉 Ако искаш нова форма -- просто добавяш нов клас, без да пипаш
старите.

------------------------------------------------------------------------

## 🟥 L --- Liskov Substitution Principle (LSP)

Всеки наследник трябва да може да замести родителя си без проблем.

### Пример:

``` ts
class Bird {
  fly() {}
}

class Eagle extends Bird {}     // ок
class Penguin extends Bird {    // НЕ ок, пингвините не летят
  fly() { throw "I can't fly"; }
}
```

❌ Примерът нарушава LSP → Penguin не може да се използва там, където се
очаква Bird.

------------------------------------------------------------------------

## 🟧 I --- Interface Segregation Principle (ISP)

По-добре много малки интерфейси, отколкото един огромен.

### ❌ Лошо:

``` ts
interface Worker {
  work(): void;
  eat(): void;
}
```

Не всеки "работник" трябва да яде (пример: робот).

### ✅ Добро:

``` ts
interface Workable {
  work(): void;
}

interface Eatable {
  eat(): void;
}
```

------------------------------------------------------------------------

## 🟨 D --- Dependency Inversion Principle (DIP)

Високо ниво код → не зависим от конкретни имплементации, а от абстракции
(интерфейси).

### ❌ Лошо:

``` ts
class FileLogger {
  log(msg: string) {}
}

class UserService {
  logger = new FileLogger(); // силна зависимост
}
```

### ✅ Добро:

``` ts
interface Logger {
  log(msg: string): void;
}

class FileLogger implements Logger {
  log(msg: string) {}
}

class UserService {
  constructor(private logger: Logger) {} // зависимост към абстракция
}
```

------------------------------------------------------------------------

## 🧠 Супер кратък начин да ги запомниш:

  Принцип   Мнемоника
  --------- -----------------------------------------------
  **S**     Един клас → една задача
  **O**     Разширяваме, не променяме
  **L**     Децата трябва да заменят родителя без проблем
  **I**     Много малки интерфейси → не един гигантски
  **D**     Зависи от абстракции, не от конкретни класове
