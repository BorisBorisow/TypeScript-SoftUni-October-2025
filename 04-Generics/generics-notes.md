# Generics

    - functions
    - interfaces
    - classes
    - type constraints
    - mapped types using generics

    Definition
        - used to build reusable software components
        - the components will work with a multitude of types instead of a single type
        - defined by type variable -  <T>
        - follow the DRY (Dont repeat yourself)
        - they can be applied to fncs, classes, interfaces, mapped types

    Generics funcs works with type unknown
